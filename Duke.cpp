#include <iostream>
#include <string>
#include <vector>

#include "Duke.hpp"


using namespace std;

namespace coup
{
    Duke::Duke(Game &game, string name) : Player(game, name, "Duke")
    {
        
    }

    void Duke::tax()
    {

    }
    
    void Duke::block(Player &player)
    {

    }
}